import * as product from './product';
import * as project from './project';
import * as utils from './utils';
import * as git from './git';
import * as terminalTask from './terminal-task';

export default {
  product,
  project,
  utils,
  git,
  terminalTask
};